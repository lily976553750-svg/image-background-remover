# Cloudflare Pages Configuration
name = "bg-remover"
compatibility_date = "2024-01-01"
pages_build_output_dir = "out"

[vars]
# Environment variables (non-secret)
# REMOVEBG_API_KEY should be set via dashboard or wrangler secret
